# Spy Cat Agency - Frontend

A **Next.js** dashboard for managing Spy Cat agents.
Features a responsive UI built with **Tailwind CSS** and seamless integration with the Spy Cat Backend API.

## 🚀 Setup

1.  **Install Dependencies:**
    ```bash
    npm install
    ```

2.  **Run Development Server:**
    ```bash
    npm run dev
    ```
    Open `http://localhost:3000` to view the dashboard.

## ⚙️ Configuration
Ensure the Backend is running on port `8000` before starting the frontend, as the API calls are directed to `http://127.0.0.1:8000`.